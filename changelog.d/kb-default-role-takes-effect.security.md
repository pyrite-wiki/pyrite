- A change to a KB's access setting now applies on the next request everywhere it is read -- the API, MCP, the public site, its sitemaps and live-update connections -- and removing an entry or a KB now removes it from the public site. Changing the setting of a KB defined in `config.yaml` through the API is refused with a clear error instead of reporting success; edit `config.yaml` and restart.
